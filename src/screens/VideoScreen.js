import { Alert, Button, Text, View } from "react-native";
import styles from "../styles/styles";

export default function VideoScreen({ navigation }) {
	return (
		<View style={styles.container}>
			<Text style={styles.title}>Video aulas</Text>
			<Text>Em breve!</Text>
		</View>
	);
}
